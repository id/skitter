# ── A2A-over-MQTT ──────────────────────────────────────
# Organisation and unit segments for A2A topic hierarchy
SKITTER_A2A_ORG=skitter
SKITTER_A2A_UNIT=default

# ── MQTT broker ──────────────────────────────────────────
MQTT_HOST=localhost
MQTT_PORT=1883

# ── LLM provider (coordinator needs one for graph generation) ──
# Set the key for your provider; litellm picks it up automatically.
# See https://docs.litellm.ai/docs/providers for all supported providers.
ANTHROPIC_API_KEY=          # for Claude models
# OPENAI_API_KEY=           # for OpenAI models
# GEMINI_API_KEY=           # for Gemini models
# DEEPSEEK_API_KEY=         # for DeepSeek models
# MOONSHOT_API_KEY=         # for Moonshot.ai models

# Model string (provider/model, litellm format).
# Also settable in ~/.skitter/config.yaml → llm.model
SKITTER_LLM_MODEL=anthropic/claude-sonnet-4-6
# SKITTER_LLM_MODEL=openai/gpt-5.4-mini
# SKITTER_LLM_MODEL=deepseek/deepseek-chat
# SKITTER_LLM_MODEL=moonshot/kimi-k2.5

# Optional: route OpenAI-compatible requests through a proxy or custom base URL.
# Use OPENAI_API_BASE to point at a local/remote OpenAI-compatible endpoint,
# or HTTPS_PROXY to tunnel requests through an HTTP proxy.
# OPENAI_API_BASE=http://localhost:8080/v1
# HTTPS_PROXY=http://localhost:3128

# Optional: if using Moonshot.ai models, set the base URL for API requests
# Default: https://api.moonshot.ai/v1
# MOONSHOT_API_BASE=https://api.moonshot.cn/v1
